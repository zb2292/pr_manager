提示词
1、Diff 视图与行级评论
问题陈述:在 Diff 视图中精确定位代码行并添加/显示评论
解决方案:利用 IntelliJ Platform 的 DiffContent API,通过 Gutter Icon 渲染评论标记
关键技术:DiffContentFactory、EditorGutterComponentEx、LineMarkerProvider
实现步骤:
1.使用 DiffContentFactory 创建文件对比视图
2.实现自定义 GutterIconRenderer 在行号旁显示评论图标
3.添加点击事件打开评论浮层或对话框
4.将评论数据与 Document 行号绑定
测试策略:测试不同文件类型的 Diff 显示,验证评论定位准确性

2、评论窗口
数据结构背景：
1、一个文件的行可以有多条评论，每个评论可视为一个评论单元；
2、评论单元中包含多个信息，每个信息视为一个信息单元，评论单元中的第一条数据，即评论的初始信息，为评论初始数据，其他出现的数据均为产生的回复数据，回复数据中一定会指向某一条已经存在的数据，而初始数据无指向；


评论主界面设计：
1、基础：行评论窗口打开可见3部分，
A.第一区域: 窗口顶部标题行，左侧显示评论图标、评论状态、评论单元个数，
B.第二区域，是一个上下滑动窗口，包含多个评论单元框体，一个评论单元是一个框体，每个框体显示默认显示3行（第一行：左侧起为用户头像+空格+人名+空格+序号+空格+评论时间，此行最右侧显示评论单元的状态 + 一个展开-合并按钮，点击后，用于展示此评论单元中的回复数据；第二行：显示评论内容，如果过长，则根据长度进行换行；第三行：提供一个回复按钮和一个问题已解决按钮（此按钮，点击后，标记当前行的评论整体状态为已解决）；
第二部分递进逻辑：
a)评论单元的回复数据，当点击展开-合并按钮，被展开后，在评论单元框体下，嵌入显示一个回复数据上下滑动窗口，包含多个回复数据框体，每个框体显示3行，第一行：左侧起为用户头像+空格+人名+空格+序号+空格+评论时间，第二行：显示评论内容，如果过长，则根据长度进行换行；第三行：提供一个回复按钮；
C.第三区域，为预评论能力，在最下方一行，从左到右显示当前用户头像，空格，人名，空格，回复按钮；

注意：提去一个统一的回复服务，评论框体中的回复、回复框体中的回复、第三区域中的回复，点击后，显示内容，处理逻辑相同；



3、PR详情

请根据以下描述，输出一份 PR 插件中“PR 详情页”的详细功能说明，包含页面结构、交互行为、状态逻辑与权限要点。页面含三个 Tab：概览、文件改动、提交记录。以下为已知细节（请完善成标准功能说明）：

通用页头区域

展示 PR 标题与编号。
展示创建人、创建时间、源分支与目标分支。
展示状态标签（例如“未解决问题 0/20”）。
操作按钮包含：评审、关闭，以及右上角更多操作菜单（含 编辑、删除）。
说明每个按钮的使用场景、权限限制（如仅创建者或管理员可编辑/删除）、状态依赖（例如关闭后不可评审等）。

Tab：概览

描述区：支持查看/编辑 PR 说明文本，含占位提示与保存策略。
关键评审人员：
可选择人员列表，支持多选。
可配置“至少需要 N 人通过”。
给出通过规则提示（如“每名关键评审成员审查通过后可合并”）。
普通评审人员：
可选择人员列表，支持多选。
可配置“至少需要 N 人通过”。
给出通过规则提示（如“每名普通评审成员审查通过后可合并”）。
合并方式：下拉选择（例如 Merged 等），描述不同合并策略的含义与影响。
合并后删除源分支：复选框，说明默认状态与影响范围。
说明表单校验、必填项、保存与回退逻辑。

Tab：文件改动

以树形结构展示文件变更：分支 → 目录 → 文件。
每个文件展示增删统计（如 +8 -2），并用颜色区分新增/删除。
支持展开/折叠目录与文件节点。
支持滚动浏览大量文件。
说明点击文件的后续行为（如打开 diff、跳转查看具体变更）。

Tab：提交记录

展示提交列表（作者、时间、提交摘要、hash）。
支持查看单次提交的变更详情。

状态与交互规则

评审通过规则如何与关键/普通评审配置关联。
未解决问题计数如何影响合并与关闭操作。
权限控制与状态变化的限制说明。




